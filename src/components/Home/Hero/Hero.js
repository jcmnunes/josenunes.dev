import React from 'react';
import styles from './Hero.css';

/**
 * ESDoc comment.
 */
const Hero = () => <div className={styles.root} />;

// // Props validation
// Hero.propTypes = {
//   firstName: React.PropTypes.string.isRequired,
//   lastName: React.PropTypes.string.isRequired,
// };

export default Hero;
